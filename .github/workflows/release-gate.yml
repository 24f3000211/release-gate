name: TDS GA7 Release Gate

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  release-gate:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"

    steps:
      - name: "TDS identity: 24f3000211@ds.study.iitm.ac.in"
        run: echo "TDS identity: 24f3000211@ds.study.iitm.ac.in"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Test release gate
        run: |
          python -c "from fastapi.testclient import TestClient; from main import app; c=TestClient(app); r=c.post('/release-gate', json={'target':'production','event':'push','ref':'refs/heads/main','workflow':{'trigger':'push','permissions':{'contents':'read','packages':'write','id-token':'none'},'testsPassed':True,'matrixComplete':True,'failFast':False,'environmentApproval':True,'actions':[{'owner':'actions','name':'checkout','ref':'v4'}]},'image':{'multiStage':True,'runsAsRoot':False,'secretMode':'none','criticalVulnerabilities':0,'digestPinned':True}}); assert r.status_code == 200; assert r.json() == {'decision':'promote','violations':[]}; print('Release gate test passed')"